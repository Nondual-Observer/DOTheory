# Cryptographic Spectral Block on the Cross-Polytope–Hypercube Composite

**Authors:** Working research draft
**Date:** April 2026
**Status:** Complete connected theorem-package for:

1. correlation immunity,
2. balancedness,
3. resilience,

on the block graph

$$
\begin{aligned}
G_{\mathrm{block}}^{(n)}
&=
O_n \cup Q_n \cup B,
\end{aligned}
$$

with analytic proofs for all $n \ge 3$ and computational verification for small dimensions.

---

## 0. Scope and Honest Status

This document collects in one place the connected spectral-cryptographic block developed around the composite graph

$$
\begin{aligned}
G_{\mathrm{block}}^{(n)}
&=
O_n \cup Q_n \cup B.
\end{aligned}
$$

Important:

- this is **not** a theorem on a restricted admissible subgraph;
- this is a theorem-package on the **full cube–cross-polytope composite**;
- it is mathematically self-contained and should be read on its own terms as a spectral theorem about an explicit composite graph.


---

## 1. The Object

Fix $n \ge 3$.

We combine three ingredients:

1. the $n$-dimensional cross-polytope graph $O_n \cong K_{2,\dots,2}$, with $2n$ pole vertices;
2. the $n$-dimensional hypercube graph $Q_n$, with $2^n$ cube vertices;
3. a binary incidence matrix $B \in \{0,1\}^{2n \times 2^n}$ coupling poles to cube vertices.

### 1.1. Pole notation

For each coordinate axis $i \in \{1,\dots,n\}$ there are two poles:

- $+e_i$,
- $-e_i$.

We index them as:

- vertex $2i$ for $+e_i$,
- vertex $2i+1$ for $-e_i$.

### 1.2. Cube vertices

Cube vertices are bit strings

$$
\begin{aligned}
s
&\in
\{0,1\}^n.
\end{aligned}
$$

### 1.3. Incidence rule

Pole $v$ with axis $i$ and sign bit $b \in \{0,1\}$ is connected to every cube vertex $s$ satisfying

$$
\begin{aligned}
s_i
&=
b.
\end{aligned}
$$

So each pole sees exactly half of the cube:

$$
\begin{aligned}
2^{n-1}
\end{aligned}
$$

vertices, and each cube vertex is incident to exactly

$$
\begin{aligned}
n
\end{aligned}
$$

poles.

---

## 2. Laplacian Form

Let:

- $m = 2n$,
- $N = 2^n$.

Let $L_{\mathrm{oct}}$ and $L_{\mathrm{cube}}$ be the Laplacians of $O_n$ and $Q_n$.

Then the Laplacian of the full block graph is:

$$
\begin{aligned}
L_{\mathrm{block}} =
\begin{pmatrix}
L_{\mathrm{oct}} + 2^{n-1}I_m & -B \\
-B^T & L_{\mathrm{cube}} + nI_N
\end{pmatrix}.
\end{aligned}
$$

The diagonal shifts come from the inter-graph degrees:

- every pole gains degree $2^{n-1}$ toward the cube,
- every cube vertex gains degree $n$ toward the poles.

---

## 3. Known Spectra of the Two Base Graphs

### 3.1. Cross-polytope graph

For

$$
\begin{aligned}
O_n
&\cong
K_{2,\dots,2},
\end{aligned}
$$

the Laplacian spectrum is:

- $\lambda = 0$ with multiplicity $1$;
- $\lambda = 2(n-1)$ with multiplicity $n$;
- $\lambda = 2n$ with multiplicity $n-1$.

The multiplicity-$n$ sector is generated by the axial vectors

$$
\begin{aligned}
\phi_i,
\end{aligned}
$$

where $\phi_i$ is:

- $+1$ on $+e_i$,
- $-1$ on $-e_i$,
- $0$ elsewhere.

### 3.2. Hypercube graph

For $Q_n$, the Laplacian spectrum is:

$$
\begin{aligned}
2k,\qquad k=0,\dots,n,
\end{aligned}
$$

with multiplicity

$$
\begin{aligned}
\binom{n}{k}.
\end{aligned}
$$

The eigenvectors are the Walsh characters

$$
\begin{aligned}
\chi_u(s)
&=
(-1)^{\langle u,s\rangle},
\end{aligned}
$$

where

$$
\begin{aligned}
u &\in \{0,1\}^n,\\
\mathrm{wt}(u)&=k.
\end{aligned}
$$

---

## 4. The Three Structural Lemmas

These three lemmas are the real engine of the whole package.

### Lemma 1. Kernel of the incidence matrix

For every Walsh character $\chi_u$:

$$
\begin{aligned}
B\chi_u = 0
&\iff
\mathrm{wt}(u)\ge 2.
\end{aligned}
$$

Equivalently:

$$
\begin{aligned}
\ker(B)
&=
\mathrm{Span}\{\chi_u:\mathrm{wt}(u)\ge 2\}.
\end{aligned}
$$

#### Meaning

The incidence matrix $B$ acts as a sieve:

- it **sees** only Walsh weight $0$ and weight $1$;
- it **annihilates** all weights $2$ and above.

This is the first decisive step.

#### Proof

Fix a pole $v=(i,b)$, where $i\in\{1,\dots,n\}$ is the axis and
$b\in\{0,1\}$ is the pole sign-bit. Then

$$
\begin{aligned}
(B\chi_u)_v
&=
\sum_{s:\,s_i=b}(-1)^{\langle u,s\rangle}.
\end{aligned}
$$

Write $s=(t_1,\dots,t_n)$ with the constraint $t_i=b$. Then

$$
\begin{aligned}
\langle u,s\rangle
&=
u_i b + \sum_{j\ne i}u_j t_j
\end{aligned}
$$

modulo $2$, hence

$$
\begin{aligned}
(B\chi_u)_v
&=
(-1)^{u_i b}
\sum_{(t_j)_{j\ne i}\in\{0,1\}^{n-1}}
\prod_{j\ne i}(-1)^{u_j t_j}.
\end{aligned}
$$

Since the variables $t_j$ are independent, the sum factorizes:

$$
\begin{aligned}
(B\chi_u)_v
&=
(-1)^{u_i b}
\prod_{j\ne i}\Bigl(\sum_{t_j\in\{0,1\}}(-1)^{u_j t_j}\Bigr).
\end{aligned}
$$

Each factor is elementary:

$$
\begin{aligned}
\sum_{t_j\in\{0,1\}}(-1)^{u_j t_j}
&=
\begin{cases}
2, & u_j=0,\\
0, & u_j=1.
\end{cases}
\end{aligned}
$$

Therefore $(B\chi_u)_v\neq 0$ if and only if all coordinates
$u_j$ with $j\ne i$ vanish. Equivalently, $u$ has weight $0$ or $1$.
If $\mathrm{wt}(u)\ge 2$, then for every pole $v$ at least one factor is zero,
so $B\chi_u=0$.

Conversely, if $\mathrm{wt}(u)=0$, then $\chi_u=\mathbf 1_N$ and
$B\chi_u=2^{n-1}\mathbf 1_m\neq 0$.
If $\mathrm{wt}(u)=1$, say $u=e_i$, then the only nonzero rows are the two poles
on axis $i$, and

$$
\begin{aligned}
(B\chi_{e_i})_{(i,0)}
&=
2^{n-1},
\qquad
(B\chi_{e_i})_{(i,1)}
&=
-2^{n-1},
\end{aligned}
$$

so again $B\chi_{e_i}\neq 0$.

Hence

$$
\begin{aligned}
\ker(B)
&=
\mathrm{Span}\{\chi_u:\mathrm{wt}(u)\ge 2\}.
\end{aligned}
$$

---

### Lemma 2. Decoupled weight-$k$ eigenvectors

If $\mathrm{wt}(u)\ge 2$, then

$$
\begin{aligned}
(0_m,\chi_u)
\end{aligned}
$$

is an eigenvector of $L_{\mathrm{block}}$ with eigenvalue

$$
\begin{aligned}
2\,\mathrm{wt}(u)+n.
\end{aligned}
$$

#### Meaning

All Walsh modes of weight at least 2 survive inside the block graph as **pure cube modes**.
They do not couple back to the octahedral side because $B$ kills them.

This gives the fully decoupled sectors:

$$
\begin{aligned}
D_k
&=
\mathrm{Span}\{(0_m,\chi_u):\mathrm{wt}(u)=k\},
\qquad k\ge 2.
\end{aligned}
$$

---

### Lemma 3. Exact axis–character coupling

For every axis $i$:

$$
\begin{aligned}
B^T\phi_i &= \chi_{e_i},\\
B\chi_{e_i} &= 2^{n-1}\phi_i.
\end{aligned}
$$

#### Meaning

Weight-1 Walsh characters are not decoupled.
Each one couples exactly to one axial mode of the cross-polytope.

This is why the weight-1 sector must be treated differently from the higher-weight sectors.

---

### Corollary 3.1. Exact rank and singular structure of the incidence matrix

The incidence matrix $B$ has:

$$
\begin{aligned}
\mathrm{rank}(B)
&=
n+1.
\end{aligned}
$$

More precisely:

$$
\begin{aligned}
\mathrm{Im}(B)
&=
\mathrm{Span}\{\mathbf 1_m,\phi_1,\dots,\phi_n\},
\end{aligned}
$$

$$
\begin{aligned}
\mathrm{Im}(B^T)
&=
\mathrm{Span}\{\mathbf 1_N,\chi_{e_1},\dots,\chi_{e_n}\},
\end{aligned}
$$

and the nonzero eigenvalues of $BB^T$ are:

$$
\begin{aligned}
n\,2^{n-1}\quad\text{with multiplicity }1,
\end{aligned}
$$

$$
\begin{aligned}
2^{n-1}\quad\text{with multiplicity }n.
\end{aligned}
$$

Equivalently, the nonzero singular values of $B$ are:

$$
\begin{aligned}
\sqrt{n\,2^{n-1}}&\quad\text{once},\\
\sqrt{2^{n-1}}&\quad\text{with multiplicity }n.
\end{aligned}
$$

#### Proof

By Lemma 1,

$$
\begin{aligned}
\ker(B)
&=
\mathrm{Span}\{\chi_u:\mathrm{wt}(u)\ge 2\}.
\end{aligned}
$$

Since the Walsh basis has total dimension $2^n$, the orthogonal complement of the kernel is

$$
\begin{aligned}
\mathrm{Span}\{\mathbf 1_N,\chi_{e_1},\dots,\chi_{e_n}\},
\end{aligned}
$$

which therefore coincides with $\mathrm{Im}(B^T)$. Hence

$$
\begin{aligned}
\mathrm{rank}(B)
&=
n+1.
\end{aligned}
$$

By Lemma 3,

$$
\begin{aligned}
B\mathbf 1_N &= 2^{n-1}\mathbf 1_m,\\
B\chi_{e_i} &= 2^{n-1}\phi_i.
\end{aligned}
$$

So $\mathrm{Im}(B)$ is contained in

$$
\begin{aligned}
\mathrm{Span}\{\mathbf 1_m,\phi_1,\dots,\phi_n\}.
\end{aligned}
$$

Since the rank is already $n+1$, this containment is an equality.

Now

$$
\begin{aligned}
B^T\mathbf 1_m
&=
n\,\mathbf 1_N,
\end{aligned}
$$

because each cube vertex is incident to exactly $n$ poles. Therefore

$$
\begin{aligned}
BB^T\mathbf 1_m
&=
B(n\mathbf 1_N)
&=
n\,2^{n-1}\mathbf 1_m.
\end{aligned}
$$

So $\mathbf 1_m$ is an eigenvector of $BB^T$ with eigenvalue $n2^{n-1}$.

Also, by Lemma 3,

$$
\begin{aligned}
BB^T\phi_i
&=
B\chi_{e_i}
&=
2^{n-1}\phi_i.
\end{aligned}
$$

Thus every axial mode $\phi_i$ is an eigenvector with eigenvalue $2^{n-1}$.

The vectors $\mathbf 1_m,\phi_1,\dots,\phi_n$ are linearly independent, their total
dimension is $n+1$, and this already equals $\mathrm{rank}(B)$. Hence these are
all nonzero eigenvalues of $BB^T$, with multiplicities exactly as stated.

`∎`

---

## 5. Theorem A. Complete Spectrum of the Block Graph

The spectrum of $L_{\mathrm{block}}$ splits into four sectors.

### 5.1. Coupled weight-0 sector

There are two weight-0 lines:

- the global constant mode,
- one nontrivial coupled line.

They give eigenvalues:

$$
\begin{aligned}
0
\quad\text{and}\quad
n+2^{n-1}.
\end{aligned}
$$

---

### 5.2. Coupled weight-1 sector

For every axis $i$, the pair

$$
\begin{aligned}
(\phi_i,\chi_{e_i})
\end{aligned}
$$

spans an invariant $2\times 2$ block:

$$
\begin{aligned}
M_1=
\begin{pmatrix}
2(n-1)+2^{n-1} & -2^{n-1}\\
-1 & n+2
\end{pmatrix}.
\end{aligned}
$$

Its two eigenvalues are:

$$
\begin{aligned}
\lambda_{w1}^{\pm}
&=
\frac{T\pm\sqrt{\Delta}}{2},
\end{aligned}
$$

where

$$
\begin{aligned}
T
&=
3n+2^{n-1},\\
\Delta
&=
(n-4+2^{n-1})^2+2^{n+1}.
\end{aligned}
$$

Each has multiplicity $n$.

---

### 5.3. Decoupled weight-$k$ sectors

For every $k\ge 2$:

$$
\begin{aligned}
\lambda
&=
2k+n
\end{aligned}
$$

with multiplicity

$$
\begin{aligned}
\binom{n}{k}.
\end{aligned}
$$

These are exactly the sectors $D_k$.

---

### 5.4. Pure octahedral sector

The orthogonal complement of the constant and axial octahedral modes gives:

$$
\begin{aligned}
\lambda
&=
2n+2^{n-1}
\end{aligned}
$$

with multiplicity $n-1$.

---

### 5.5. Full spectrum table

| Sector | Eigenvalue | Multiplicity |
|:---|:---|:---:|
| Coupled weight-0 | $0$ | 1 |
| Coupled weight-0 | $n+2^{n-1}$ | 1 |
| Coupled weight-1 | $\lambda_{w1}^{-}$ | $n$ |
| Coupled weight-1 | $\lambda_{w1}^{+}$ | $n$ |
| Decoupled weight-$k$ | $2k+n$ | $\binom{n}{k}$ |
| Pure octahedral | $2n+2^{n-1}$ | $n-1$ |

The multiplicities sum to:

$$
\begin{aligned}
1+1+n+n+\sum_{k=2}^n\binom{n}{k}+(n-1)
&=
2n+2^n.
\end{aligned}
$$

So the decomposition is complete.

---

## 6. Boolean Functions and the Embedded Vector

Let

$$
\begin{aligned}
f:\{0,1\}^n\to\{0,1\}
\end{aligned}
$$

be a Boolean function.

Its bipolar form is:

$$
\begin{aligned}
f_{\pm}
&=
2f-1
\in
\{-1,+1\}^{2^n}.
\end{aligned}
$$

We embed it into the block graph as:

$$
\begin{aligned}
f_{\mathrm{block}}
&=
(0_m,f_{\pm}).
\end{aligned}
$$

This is the universal fixed-graph viewpoint:

- the graph is fixed;
- the function does **not** define the graph;
- instead, the function is inserted into the cube-side of one common spectral object.

---

## 7. Theorem B. Correlation Immunity as Spectral Stratification

Recall the standard Walsh criterion:

$$
\begin{aligned}
\mathrm{CI}(f)\ge t
&\iff
W_f(u)=0
\quad\text{for all }u\text{ with }1\le \mathrm{wt}(u)\le t.
\end{aligned}
$$

In the block graph, this becomes:

$$
\boxed{
\begin{aligned}
\mathrm{CI}(f)\ge t
&\iff
\mathrm{proj}(f_{\mathrm{block}},E_{\lambda_{w1}^{-}})=0\\
&\quad\wedge\;
\forall k\in[2,t]:
\mathrm{proj}(f_{\mathrm{block}},D_k)=0.
\end{aligned}}
$$

For $n\ge 5$, where the relevant sectors are spectrally separated, this can be written as:

$$
\boxed{
\begin{aligned}
\mathrm{CI}(f)\ge t
&\iff
\mathrm{proj}(f_{\mathrm{block}},E_{\lambda_{w1}^{-}})=0\\
&\quad\wedge\;
\forall k\in[2,t]:
\mathrm{proj}(f_{\mathrm{block}},E_{\lambda=2k+n})=0.
\end{aligned}}
$$

The simplification for $n\ge 5$ is justified by the explicit spectral ladder

$$
\begin{aligned}
\lambda_{w1}^{-}
&< n+2 < n+4 \le 2k+n \le 3n\\
&< n+2^{n-1} < 2n+2^{n-1},
\end{aligned}
$$

valid for every $k\ge 2$.
Indeed:

- from $\sqrt{(n-4+2^{n-1})^2+2^{n+1}} > n-4+2^{n-1}$, we get $\lambda_{w1}^{-} < n+2$;
- for $k\ge 2$, one has $2k+n \ge n+4$;
- for $n\ge 5$, one has $3n < n+2^{n-1}$ because $2^{n-1} > 2n$.

So from dimension $5$ onward the coupled weight-$1$ sector, the decoupled
weight-$k$ sectors, the coupled weight-$0$ line, and the pure octahedral
sector occupy pairwise distinct eigenvalues.

### Why this is true

- weight 1 is controlled by the coupled axial sector from Lemma 3;
- weights $2,\dots,t$ are controlled by the decoupled sectors from Lemma 2.

So the usual Walsh vanishing condition becomes vanishing on explicit spectral sectors of $L_{\mathrm{block}}$.

### 7.1. Why low-dimensional collisions are harmless

For $n=3$ and $n=4$, some eigenvalues collide:

- for $n=3$, the nontrivial weight-0 line $C_0^+$ and the decoupled sector $D_2$
  both lie at eigenvalue $\lambda=7$;
- for $n=4$, the same phenomenon occurs for $C_0^+$ and $D_4$, both lying at
  eigenvalue $\lambda=12$.

This does **not** invalidate Theorem B, because the theorem is stated using the
canonical decoupled sectors $D_k$, not the full eigenspaces $E_{\lambda=2k+n}$.
The two colliding pieces remain canonically separated.

Indeed:

- every vector in $D_k$ has the form $(0_m,\chi_u)$ and therefore has zero
  octahedral component;
- the vector spanning $C_0^+$ is $(2^{n-1}\mathbf 1_m,-n\mathbf 1_N)$, whose octahedral component is nonzero.

Therefore

$$
\begin{aligned}
C_0^+\cap D_k
&=
\{0\}
\end{aligned}
$$

in the colliding cases, so the common eigenspace still decomposes as a direct sum
of two canonically distinguishable pieces:

$$
\begin{aligned}
E_{\lambda}
&=
C_0^+\oplus D_k.
\end{aligned}
$$

This is exactly why the all-$n$ formulation of Theorem B is written with
the decoupled sectors $D_k$, whereas the simplified eigenspace formulation is
reserved for $n\ge 5$.

For weight $1$ there is no analogous ambiguity: the coupled sector is generated by
the $n$ invariant $2\times 2$ blocks from Lemma 3, and its cube-side projections
span the full weight-1 Walsh space. Hence the CI criterion remains exact in
dimensions $n=3,4$, even though some higher-weight cube sectors may collide in
eigenvalue with the nontrivial weight-0 line.

### Meaning

Correlation immunity is no longer read only as a Walsh-table condition.
It becomes a **spectral non-resonance condition** with specific low-weight sectors of the block graph.

---

## 8. The Weight-0 Completion

The CI theorem controls Walsh weights

$$
\begin{aligned}
1,\dots,t,
\end{aligned}
$$

but not weight

$$
\begin{aligned}
0.
\end{aligned}
$$

Weight 0 is exactly what controls balancedness.

So to complete the low-weight package, we need the exact weight-0 sector.

### 8.1. Two coupled weight-0 lines

Define:

$$
\begin{aligned}
C_0^-
&=
\mathrm{Span}\{(\mathbf 1_m,\mathbf 1_N)\},
\end{aligned}
$$

$$
\begin{aligned}
C_0^+
&=
\mathrm{Span}\{(2^{n-1}\mathbf 1_m,-n\mathbf 1_N)\}.
\end{aligned}
$$

The first is the global constant mode.
The second is the nontrivial coupled weight-0 line.

---

## 9. Theorem C. Balancedness as a Spectral Condition

Balancedness is equivalent to:

$$
\begin{aligned}
\sum_x f_{\pm}(x)
&=
0.
\end{aligned}
$$

Since

$$
\begin{aligned}
f_{\mathrm{block}}
&=
(0_m,f_{\pm}),
\end{aligned}
$$

we have:

$$
\begin{aligned}
\langle f_{\mathrm{block}},(\mathbf 1_m,\mathbf 1_N)\rangle
&=
\sum_x f_{\pm}(x),
\end{aligned}
$$

and also

$$
\begin{aligned}
\langle f_{\mathrm{block}},(2^{n-1}\mathbf 1_m,-n\mathbf 1_N)\rangle
&=
-n\sum_x f_{\pm}(x).
\end{aligned}
$$

Therefore:

$$
\boxed{
\begin{aligned}
f\text{ is balanced}
&\iff
\mathrm{proj}(f_{\mathrm{block}},C_0^-)=0\\
&\iff
\mathrm{proj}(f_{\mathrm{block}},C_0^+)=0.
\end{aligned}}
$$

For $n\ge 5$, the line $C_0^+$ is exactly the eigenspace

$$
\begin{aligned}
E_{\lambda=n+2^{n-1}}.
\end{aligned}
$$

### Meaning

Balancedness is the missing weight-0 part of the same spectral picture.

---

## 10. Theorem D. Resilience as the Completed Low-Weight Package

By definition:

$$
\begin{aligned}
\mathrm{Res}(f)\ge t
&\iff
f\text{ is balanced and }\mathrm{CI}(f)\ge t.
\end{aligned}
$$

Combining Theorem B and Theorem C, we obtain:

$$
\boxed{
\begin{aligned}
\mathrm{Res}(f)\ge t
&\iff
\mathrm{proj}(f_{\mathrm{block}},C_0^+)=0\\
&\quad\wedge\;
\mathrm{proj}(f_{\mathrm{block}},E_{\lambda_{w1}^{-}})=0\\
&\quad\wedge\;
\forall k\in[2,t]:
\mathrm{proj}(f_{\mathrm{block}},D_k)=0.
\end{aligned}}
$$

For $n\ge 5$, this becomes a pure eigenspace statement:

$$
\boxed{
\begin{aligned}
\mathrm{Res}(f)\ge t
&\iff
\mathrm{proj}(f_{\mathrm{block}},E_{\lambda=n+2^{n-1}})=0\\
&\quad\wedge\;
\mathrm{proj}(f_{\mathrm{block}},E_{\lambda_{w1}^{-}})=0\\
&\quad\wedge\;
\forall k\in[2,t]:
\mathrm{proj}(f_{\mathrm{block}},E_{\lambda=2k+n})=0.
\end{aligned}}
$$

### Meaning

Resilience is exactly the statement that the embedded Boolean function has no spectral mass in the entire dangerous low-weight package:

1. weight 0,
2. weight 1,
3. weights $2,\dots,t$.

So the current block graph detects the whole low-weight Walsh package:

- balancedness,
- correlation immunity,
- resilience.

---

## 11. What This Block Really Achieves

The real value of this theorem-package is not that it invents new cryptographic properties.
These properties are classical.

The value is that it gives a **new spectral-geometric packaging**:

- one fixed graph,
- one fixed Laplacian,
- one exact sector decomposition,
- multiple standard Boolean properties recovered as vanishing of explicit sectors.

That is the genuine conceptual gain.

### 11.1. Context and novelty claim

The novelty claim here should be read in a precise and limited sense.

This document does **not** claim to introduce correlation immunity, balancedness,
or resilience as cryptographic properties. Those are classical. It also does not
claim to supersede the standard Walsh/Fourier criteria.

What is new here is the following package:

1. one fixed composite graph $G_{\mathrm{block}}^{(n)}=O_n\cup Q_n\cup B$,

2. one explicit Laplacian with complete analytic spectrum,
3. one spectral sector decomposition in which low-weight Walsh data is read as
   vanishing on concrete coupled or decoupled graph sectors.

So the novelty is a **fixed-graph spectral reformulation** of a low-weight Walsh
package, rather than the discovery of a new Boolean invariant.

This positioning is intentionally cautious: it should be understood as a new
graph-theoretic packaging of standard cryptographic conditions, not as a claim
that no related spectral encoding has ever appeared elsewhere in the literature.

### 11.2. Immediate directions suggested by the construction

The present block naturally suggests several nearby continuations:

1. replacing the current pole-to-cube coupling rule by other explicit incidence
   designs and comparing the resulting low-weight sector filters;
2. testing whether other standard cryptographic properties can be rewritten in
   the same language of vanishing or support on block-graph sectors;
3. studying whether the sector decomposition can be used as a classification
   device for families of Boolean functions with prescribed low-weight Walsh
   behavior.

These directions are not needed for the current proofs, but they explain why the
block is of independent interest beyond the single CI statement.

---

## 12. Honest Boundaries

This block does **not** claim:

- a theorem on a smaller restricted subgraph obtained by deleting boundary vertices;
- a new universal cryptographic framework;
- an algorithmic speedup over Walsh computation;
- any physical interpretation.

It **does** claim:

- a new explicit composite graph;
- its complete analytic spectrum;
- an exact spectral-geometric reformulation of:
  - correlation immunity,
  - balancedness,
  - resilience.

---

## 13. Verification Scope

The companion verification script checks:

1. Lemma 1: exact kernel of $B$;
2. Lemma 2: decoupled eigenvectors;
3. Lemma 3: exact axis–character coupling;
4. full analytic spectrum;
5. low-dimensional collision consistency;
6. CI criterion;
7. weight-0 criterion;
8. resilience criterion.

The verification is:

- exhaustive for $n=3$ and $n=4$ over all nontrivial Boolean functions;
- structural/spectral for $n=5,\dots,8$.

---

In short:

> the low-weight Walsh structure of a Boolean function can be read as a spectral stratification on a single explicit cross-polytope–hypercube composite.
